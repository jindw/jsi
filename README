JavaScript Introduction

Functions
 * CommonJS1.0 compatible JavaScript loader [ require.js]
   Rewriting of the original [JSI2](http://www.xidea.org/project/jsi) boot.js, give up the original specification, former JSI2 tool set is being reconstructed, later on
 * generate browser script wrapper
   In order to make the scripts installed by NPM can be "require"d in the browser environment , it adds an automatic processor, the script into similar AMD model.
 * generate different code for different agent (browser)
		
Install:
	npm install jsi
Example:
	require('jsi/test/run');


