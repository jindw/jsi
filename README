Introduction
JavaScript Static Service

Functions
	1. javascript dependence management system form jsi2.0 and refactor as CommonJS 1.0 compatibility
	2. auto browser load wrapper for nodejs compatibility(commonjs and npm).
	3. auto css3/javascript browser indepence deference expression support.
	4. auto css3 selector and animation support for ie6 

Install:
	npm install jsi

Example:
	var jsi = require('jsi/test/run');
	jsi.run();


